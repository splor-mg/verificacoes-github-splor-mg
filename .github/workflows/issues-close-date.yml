name: issue-closed-sync

on:
  issues:
    types: [closed]
  workflow_dispatch:
    inputs:
      update_project_list:
        description: 'Atualizar lista de projetos antes de executar?'
        required: false
        default: 'não'
        type: choice
        options:
          - 'sim'
          - 'não'
      organization:
        description: 'Organização na qual atualizar issues'
        required: true
        default: 'splor-mg'
      project_number:
        description: 'Projectos nos quais atualizar issues'
        required: true
        default: "13 - Gestão à Vista AID"
        type: choice
        options:
          - "13 - Gestão à Vista AID"
          - "16 - Gestão Gabinete SPLOR"
      field_name:
        description: "Project Date field to set"
        required: true
        default: "Data Fim"
      period_to_apply:
        description: "Processar criados/modificados no período"
        required: true
        default: "1 - últimos 7 dias"
        type: choice
        options:
          - "1 - últimos 7 dias"
          - "2 - últimos 30 dias"
          - "3 - todos"

jobs:
  set-end-date:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: read
    env:
      GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
      GITHUB_ORG: ${{ inputs.organization }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GH_TOKEN }}

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install Poetry
        uses: snok/install-poetry@v1
        with:
          version: latest
          virtualenvs-create: true
          virtualenvs-in-project: true

      - name: Load cached venv
        id: cached-poetry-dependencies
        uses: actions/cache@v3
        with:
          path: .venv
          key: venv-${{ runner.os }}-3.11-${{ hashFiles('**/poetry.lock') }}

      - name: Install dependencies
        if: steps.cached-poetry-dependencies.outputs.cache-hit != 'true'
        run: poetry install --no-interaction --no-root

      - name: Activate Poetry environment
        run: |
          echo "POETRY_VENV_PATH=$(poetry env info --path)" >> $GITHUB_ENV
          echo "$(poetry env info --path)/bin" >> $GITHUB_PATH

      - name: Update projects data
        if: inputs.update_project_list == 'sim'
        run: |
          echo "🔄 Atualizando dados dos projetos..."
          poetry run python main.py --projects-panels --projects-list
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
          GITHUB_ORG: ${{ inputs.organization }}

      - name: Update workflow options dynamically
        if: inputs.update_project_list == 'sim'
        run: |
          echo "🔄 Atualizando opções de projetos dinamicamente..."
          poetry run python scripts/update_workflow_options.py
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
          GITHUB_ORG: ${{ inputs.organization }}

      - name: Commit and push project updates
        if: inputs.update_project_list == 'sim'
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add docs/ .github/workflows/issues-close-date.yml
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Update project options for ${{ inputs.organization }} [skip ci]"
            git push
          fi

      - name: Stop workflow after project update
        if: inputs.update_project_list == 'sim'
        run: |
          echo "✅ Lista de projetos atualizada com sucesso!"
          echo "💡 Execute o workflow novamente com 'Atualizar lista de projetos' = 'Não' para gerenciar issues"
          exit 0

      - name: Extract project number from selection
        if: inputs.update_project_list == 'não'
        id: extract_project
        run: |
          # Extrair número do projeto da seleção (ex: "1 - Gestão à Vista Pilot" -> "1")
          PROJECT_SELECTION="${{ inputs.project_number }}"
          PROJECT_NUMBER=$(echo "$PROJECT_SELECTION" | cut -d' ' -f1)
          echo "project_number=$PROJECT_NUMBER" >> $GITHUB_OUTPUT
          echo "Selected project: $PROJECT_SELECTION (Number: $PROJECT_NUMBER)"

      - name: Process period selection
        if: inputs.update_project_list == 'não'
        id: process_period
        run: |
          # Processar seleção de período
          PERIOD_SELECTION="${{ inputs.period_to_apply }}"
          case "$PERIOD_SELECTION" in
            "1 - últimos 7 dias")
              echo "period_args=" >> $GITHUB_OUTPUT
              echo "period_description=últimos 7 dias" >> $GITHUB_OUTPUT
              ;;
            "2 - últimos 30 dias")
              echo "period_args=--days 30" >> $GITHUB_OUTPUT
              echo "period_description=últimos 30 dias" >> $GITHUB_OUTPUT
              ;;
            "3 - todos")
              echo "period_args=--all-issues" >> $GITHUB_OUTPUT
              echo "period_description=todos os issues" >> $GITHUB_OUTPUT
              ;;
            *)
              echo "period_args=" >> $GITHUB_OUTPUT
              echo "period_description=últimos 7 dias (padrão)" >> $GITHUB_OUTPUT
              ;;
          esac
          echo "Processando issues: $PERIOD_SELECTION"

      - name: Sync Project field on issue close
        if: inputs.update_project_list == 'não'
        run: |
          echo "🔧 Gerenciando campo '${{ inputs.field_name }}' para projeto ${{ steps.extract_project.outputs.project_number }}"
          echo "📅 Período: ${{ steps.process_period.outputs.period_description }}"
          poetry run python main.py \
            --issues-close-date \
            --issues-projects "${{ steps.extract_project.outputs.project_number }}" \
            --issues-field "${{ inputs.field_name }}" \
            --org "${{ inputs.organization }}" \
            ${{ steps.process_period.outputs.period_args }}
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
          GITHUB_ORG: ${{ inputs.organization }}

      - name: Commit and push changes
        if: inputs.update_project_list == 'não'
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add docs/
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Update projects data for ${{ inputs.organization }} [skip ci]"
            git push
          fi


