name: issue-closed-sync

on:
  schedule:
    - cron: '47 23 * * 1-5'  # Segunda à sexta às 23:47
  issues:
    types: [closed]
  workflow_dispatch:
    inputs:
      update_project_list:
        description: 'Atualizar lista de projetos antes de executar?'
        required: false
        default: 'não'
        type: choice
        options:
          - 'sim'
          - 'não'
      organization:
        description: 'Organization name to sync issues for'
        required: true
        default: 'aid-pilot'
      project_number:
        description: "Project to sync issues for"
        required: true
        default: "13 - Gestão à Vista AID"
        type: choice
        options:
          - "13 - Gestão à Vista AID"
          - "16 - Gestão Gabinete SPLOR"
      field_name:
        description: "Project Date field to set"
        required: true
        default: "Data Fim"
      days:
        description: "Processar issues atualizados nos últimos N dias (0 = todos)"
        required: false
        default: "7"
      all_issues:
        description: "Processar todos os issues (ignora 'days')"
        required: false
        type: boolean
        default: false

jobs:
  set-end-date:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      issues: read
    env:
      GITHUB_ORG: ${{ inputs.organization }}
    steps:
      - name: Generate GitHub App token
        id: app-token
        uses: actions/create-github-app-token@v2
        with:
          app-id: ${{ secrets.GH_APP_ID }}
          private-key: ${{ secrets.GH_APP_PRIVATE_KEY }}
          owner: ${{ inputs.organization }}

      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ steps.app-token.outputs.token }}

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install Poetry
        uses: snok/install-poetry@v1
        with:
          version: latest
          virtualenvs-create: true
          virtualenvs-in-project: true

      - name: Load cached venv
        id: cached-poetry-dependencies
        uses: actions/cache@v3
        with:
          path: .venv
          key: venv-${{ runner.os }}-3.11-${{ hashFiles('**/poetry.lock') }}

      - name: Install dependencies
        if: steps.cached-poetry-dependencies.outputs.cache-hit != 'true'
        run: poetry install --no-interaction --no-root

      - name: Activate Poetry environment
        run: |
          echo "POETRY_VENV_PATH=$(poetry env info --path)" >> $GITHUB_ENV
          echo "$(poetry env info --path)/bin" >> $GITHUB_PATH

      - name: Update projects data
        if: inputs.update_project_list == 'sim'
        run: |
          echo "🔄 Atualizando dados dos projetos..."
          poetry run python main.py --projects-panels-update
        env:
          GITHUB_TOKEN: ${{ steps.app-token.outputs.token }}
          GITHUB_ORG: ${{ inputs.organization }}
          GITHUB_APP_ID: ${{ secrets.GH_APP_ID }}
          GITHUB_APP_INSTALLATION_ID: ${{ secrets.GH_APP_INSTALLATION_ID }}
          GITHUB_APP_PRIVATE_KEY: ${{ secrets.GH_APP_PRIVATE_KEY }}

      - name: Update workflow options dynamically
        if: inputs.update_project_list == 'sim'
        run: |
          echo "🔄 Atualizando opções de projetos dinamicamente..."
          poetry run python scripts/update_workflow_options.py
        env:
          GITHUB_TOKEN: ${{ steps.app-token.outputs.token }}
          GITHUB_ORG: ${{ inputs.organization }}
          GITHUB_APP_ID: ${{ secrets.GH_APP_ID }}
          GITHUB_APP_INSTALLATION_ID: ${{ secrets.GH_APP_INSTALLATION_ID }}
          GITHUB_APP_PRIVATE_KEY: ${{ secrets.GH_APP_PRIVATE_KEY }}

      - name: Commit and push project updates
        if: inputs.update_project_list == 'sim'
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add config/ .github/workflows/issues-close-date.yml
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Update project options for ${{ inputs.organization }} [skip ci]"
            git push
          fi

      - name: Stop workflow after project update
        if: inputs.update_project_list == 'sim'
        run: |
          echo "✅ Lista de projetos atualizada com sucesso!"
          echo "💡 Execute o workflow novamente com 'Atualizar lista de projetos' = 'Não' para gerenciar issues"
          exit 0

      - name: Extract project number from selection
        if: inputs.update_project_list == 'não'
        id: extract_project
        run: |
          # Extrair número do projeto da seleção (ex: "1 - Gestão à Vista Pilot" -> "1")
          PROJECT_SELECTION="${{ inputs.project_number }}"
          PROJECT_NUMBER=$(echo "$PROJECT_SELECTION" | cut -d' ' -f1)
          echo "project_number=$PROJECT_NUMBER" >> $GITHUB_OUTPUT
          echo "Selected project: $PROJECT_SELECTION (Number: $PROJECT_NUMBER)"

      - name: Sync Project field on issue close
        if: inputs.update_project_list == 'não'
        run: |
          echo "🔧 Gerenciando campo '${{ inputs.field_name }}' para projeto ${{ steps.extract_project.outputs.project_number }}"
          poetry run python main.py \
            --issues-close-date \
            --issues-close-date-panels "${{ steps.extract_project.outputs.project_number }}" \
            --issues-close-date-field "${{ inputs.field_name }}" \
            --org "${{ inputs.organization }}" \
            ${{ inputs.all_issues && '--issues-all' || '' }} \
            ${{ !inputs.all_issues && format('--issues-days {0}', inputs.days) || '' }}
        env:
          GITHUB_TOKEN: ${{ steps.app-token.outputs.token }}
          GITHUB_ORG: ${{ inputs.organization }}
          GITHUB_APP_ID: ${{ secrets.GH_APP_ID }}
          GITHUB_APP_INSTALLATION_ID: ${{ secrets.GH_APP_INSTALLATION_ID }}
          GITHUB_APP_PRIVATE_KEY: ${{ secrets.GH_APP_PRIVATE_KEY }}

      - name: Commit and push changes
        if: inputs.update_project_list == 'não'
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add config/
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Update projects  ${{ inputs.field_name }} for ${{ inputs.organization }} [skip ci]"
            git push
          fi


