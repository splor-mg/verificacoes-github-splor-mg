# CONFIGURAÇÃO DO AMBIENTE
 
# INSTRUÇÕES:
# 1. Copie todo o conteúdo deste arquivo e salve em um novo arquivo chamado ".env" (sem extensão)
 
# ```bash
# cp .env.example .env
# ``` 
# 2. Edite as variáveis abaixo conforme necessário
# 3. O arquivo .env será ignorado pelo Git por segurança

# Autenticação via GitHub App (recomendado)
# Preencha as variáveis abaixo para que o projeto gere tokens de instalação automaticamente
# a partir do seu GitHub App.

# Como obter:
# 1. Crie um GitHub App e instale-o na organização
# 2. Anote o App ID e o Installation ID
# 3. Baixe a chave privada (.pem) do App
# 4. Aponte para o arquivo ou cole o conteúdo como variável

# Obrigatórias
GITHUB_APP_ID=
GITHUB_APP_INSTALLATION_ID=

# UMA das duas a seguir:
# - Caminho para o arquivo .pem (recomendado localmente)
GITHUB_APP_PRIVATE_KEY_PATH=/home/seuusuario/.config/splor-mg/github-apps/app-verificacoes-github/private-key.pem
# - Conteúdo da chave (PEM) ou base64 do PEM
#GITHUB_APP_PRIVATE_KEY=

# Nome da organização no GitHub
GITHUB_ORG=splor-mg

# Arquivo de configuração das labels padrão
# Mantenha este valor se estiver usando a estrutura padrão do projeto
GITHUB_LABELS_FILE=config/labels.yaml

# Projeto/painel padrão para sincronização (número do projeto)
# Use o número do projeto conforme listado em docs/projects-panels-list.yml, por padrão, definido como 13
GITHUB_PROJECT_PANEL_DEFAULT=13
